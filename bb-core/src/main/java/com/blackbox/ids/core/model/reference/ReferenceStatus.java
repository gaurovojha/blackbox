package com.blackbox.ids.core.model.reference;

/**
 * The Enum ReferenceStatus.
 */
public enum ReferenceStatus {
	/** The new. */
	NEW,
	/** The active. */
	ACTIVE,
	/** The duplicate. */
	DUPLICATE,
	/** The pending. */
	PENDING,
	/** The source ref. */
	SOURCE_REF,
	/** The office action. */
	OFFICE_ACTION,
	/** The dropped. */
	DROPPED,
	/** The inpadoc failed. */
	INPADOC_FAILED,
	/** The import ready. */
	IMPORT_READY,
	/** The import done. */
	IMPORT_DONE,
	/** The notification sent. */
	NOTIFICATION_SENT,
	/** The inpadoc pendings. */
	INPADOC_PENDING,
}
