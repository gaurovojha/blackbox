/** namespace.js defines the namespace for Blackbox webapp. */
(function( $ ) {
	
	// Application level package
	$.blackbox = {};
	
	// Blackbox front end utility package
	$.blackbox.util = {};
	
	// Blackbox front end validation package.
	$.blackbox.validator = {};
	
	// Blackbox common package.
	$.blackbox.common = {};
	
})( jQuery );